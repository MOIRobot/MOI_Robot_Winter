<launch>
  <node pkg="leg_detector" type="leg_detector" name="leg_detector" args="scan:=base_scan $(find leg_detector)/config/trained_leg_detector.yaml">
		<param name="use_seeds" value="false" />
		<param name="fixed_frame" value="/odom" />
	</node>
</launch>

